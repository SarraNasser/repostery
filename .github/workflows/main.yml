on:
  push:
    branches:
      - master






jobs:
  build:
    runs-on: ubuntu-latest

   
      - name: "testing git hub"
        run: pwd 
       -name: "testin printing"
         run: echo sarra